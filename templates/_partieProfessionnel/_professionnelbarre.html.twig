<div class="container mx-auto">
    <div class="flex flex-col md:flex-row">
        <aside class="w-full md:w-72 border-l border-gray-200">
            <div class="p-4">
                <h2 class="text-xl font-semibold mb-4 flex items-center">
                    <i class="fas fa-store mr-2"></i>
                    Gestion de ma boutique
                </h2>
                <ul class="space-y-2">
                    <li>
                        <a href="{{ path('app_boutique_edit', {'id': boutique.id}) }}" class="flex items-center text-blue-600 hover:text-blue-800">
                            <i class="fas fa-edit mr-2"></i>
                            Gérer les informations de ma boutique
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_commande_recue', {'id': boutique.id}) }}" class="flex items-center text-blue-600 hover:text-blue-800">
                            <i class="fas fa-edit mr-2"></i>
                            Gérer mes commandes
                        </a>
                    </li>
                </ul>
            </div>
            <div class="p-4 mt-4">
                <h2 class="text-xl font-semibold mb-4 flex items-center">
                    <i class="fas fa-boxes mr-2"></i>
                    Produits
                </h2>
                <ul class="space-y-2">
                    <li>
                        <a href="{{ path('app_produit_new') }}" class="flex items-center text-blue-600 hover:text-blue-800">
                            <i class="fas fa-plus-circle mr-2"></i>
                            Ajouter un produit à ma boutique
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_produit_boutique') }}" class="flex items-center text-blue-600 hover:text-blue-800">
                            <i class="fas fa-list mr-2"></i>
                            Tous les produits de ma boutique
                        </a>
                    </li>
                </ul>
            </div>
        </aside>
    </div>
</div>