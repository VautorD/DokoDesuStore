{% extends 'base.html.twig' %}

{% block title %}Edit Abonnement
{% endblock %}

{% block body %}
	<div class="container mx-auto px-4 py-8">
		<div class="bg-white shadow overflow-hidden sm:rounded-lg">
			<div class="px-4 py-5 sm:px-6">
				<h1 class="text-2xl font-semibold text-gray-800">Modifier l'abonnement
				</h1>
			</div>

			{{ include('abonnement/_form.html.twig', {'button_label': 'Modifier'}) }}
				<a href="{{ path('app_abonnement_index') }}" class="inline-block text-sm ml-4 mb-4 px-4 py-2 leading-none border rounded-lg text-primary border-primary hover:border-transparent hover:text-white hover:bg-primary mt-4 lg:mt-0">Retourner à la liste</a>

		</div>
	</div>
	<div class="ml-4 my-4">

		{{ include('abonnement/_delete_form.html.twig') }}
	</div>
{% endblock %}
