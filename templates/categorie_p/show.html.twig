{% extends 'base.html.twig' %}

{% block title %}CategorieP{% endblock %}

{% block body %}
    <h1>CategorieP</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_p.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_p.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_p.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categorie_p.slug }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ categorie_p.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_p_index') }}">back to list</a>

    <a href="{{ path('app_categorie_p_edit', {'id': categorie_p.id}) }}">edit</a>

    {{ include('categorie_p/_delete_form.html.twig') }}
{% endblock %}
